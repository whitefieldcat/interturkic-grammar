\documentclass[10pt,a4paper]{memoir}

\title{Interturkic Grammar}
\author{whitefieldcat}
\date{TBA}

% Set margins proportional to paper size.
\setlrmarginsandblock{0.125\paperwidth}{0.25\paperwidth}{*}
\setulmarginsandblock{0.125\paperheight}{0.25\paperheight}{*}
\checkandfixthelayout

% Disable uppercasing of chapter/section names in page headers.
\nouppercaseheads

% Number and include headings.
\setsecnumdepth{all}
\settocdepth{all}

% Font selection.
\usepackage{fontspec}
\setmainfont{NotoSerif}
\newfontfamily{\latnfont}{NotoSerif}[HyphenChar=None,Ligatures=CommonOff]
\newfontfamily{\arabfont}{NotoNaskhArabic}[Renderer=HarfBuzz,Script=Arabic]
\newfontfamily{\symbfont}{NotoSansMath}

% Bidirectional typesetting.
\usepackage{luabidi}

% Microtypographical refinements.
\usepackage{microtype}

% Fine-grained hyphenation at dashes.
\usepackage[shortcuts]{extdash}

% Formatting of examples and interlinear glosses
\usepackage{expex}
\lingset{aboveglftskip=0pt,everygla=\latnfont\it,numoffset=\parindent}

% Fancy horizontal rules in tables.
\usepackage{booktabs}

% Multipage tables.
\usepackage{longtable}

% Mulirow cells in tables.
\usepackage{multirow}

% Smart references to labels. Must be loaded before hyperref.
\usepackage[nospace]{varioref}

% Hyperlinks and PDF metadata. Should be among the last packages loaded.
\usepackage[hidelinks,hypertexnames=false,pdfdisplaydoctitle,pdfusetitle]
  {hyperref}
\pdfstringdefDisableCommands{\def\-/{-}}

% PDF bookmark management. Must be loaded after hyperref.
\usepackage{bookmark}

% Glossing abbreviations. Must be loaded after hyperref.
\usepackage[leipzighyper,mcolblock,toc]{leipzig}
\glsfindwidesttoplevelname
\makeglossaries

% Titling pages.
\newcommand{\makehalftitle}{%
  \cleartorecto
  \begin{titlingpage*}
    \centering
    \vspace*{\fill}
    {\HUGE\thetitle}\par
    \vspace*{\fill}
  \end{titlingpage*}}
\newcommand{\makefulltitle}{%
  \cleartorecto
  \begin{titlingpage*}
    \centering
    \vspace*{\fill}
    {\HUGE\thetitle}\par
    \vspace*{\fill}
    \theauthor\par
    \thedate\par
  \end{titlingpage*}}

% Content lists
\renewcommand{\contentsname}{Table of contents}
\newcommand{\maketoc}{%
  \cleartorecto
  \pdfbookmark{\contentsname}{toc}
  \begin{KeepFromToc}
    \tableofcontents
  \end{KeepFromToc}}
\renewcommand{\leipzigname}{List of abbreviations}
\newcommand{\makeloa}{%
  \cleartorecto
  \printglossaries}
\renewcommand{\listtablename}{List of tables}
\newcommand{\makelot}{%
  \cleartorecto
  \listoftables}

% Forbid widow/orphan lines.
\clubpenalty=10000
\widowpenalty=10000

% Phonemic transcriptions.
\newcommand{\phonem}[1]{/\nolinebreak#1\nolinebreak/}
% Phonetic transcriptions.
\newcommand{\phonet}[1]{[\nolinebreak#1\nolinebreak]}

% Latin-script foreign-language text (when embedded in English-language prose).
\newcommand{\foreign}[1]{\emph{#1}}
% Latin-script Interturkic text.
\newcommand{\latntxt}[1]{{\latnfont#1}}
% Arabic-script text.
\newcommand{\arabtxt}[1]{\RL{\arabfont#1}}

% Intralinear glosses.
\newcommand{\intragls}[1]{{\symbfont⟨}\nolinebreak#1\nolinebreak{\symbfont⟩}}
% Interlinear glosses.
\newcommand{\intergls}[3]{%
  \begingl
    \gla #1//
    \glb #2//
    \glft `#3'//
  \endgl}

% Miscellaneous symbols.
\newcommand{\derivesto}{{\symbfont→}}
\newcommand{\derivesfrom}{{\symbfont←}}
\newcommand{\copiesto}{{\symbfont⇒}}
\newcommand{\copiesfrom}{{\symbfont⇐}}
\newcommand{\zero}{{\symbfont∅}}
\newcommand{\altwith}{\textasciitilde}

\begin{document}
  \makehalftitle
  \makefulltitle

  \frontmatter

  \maketoc
  \makeloa
  \makelot

  \mainmatter

  \part{Phonology}

  \part{Orthography}

  \part{Morphology}

  \part{Syntax}
\end{document}